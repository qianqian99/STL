#if 1
#include <iostream>
#include <vector>
using namespace std;
int main() {
    vector<int> v(2, 8);
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);
    v.push_back(4);
    cout << "size : " << v.size() << " capacity: " << v.capacity() << endl; 
    return 0;
}
#else
www
#endif
